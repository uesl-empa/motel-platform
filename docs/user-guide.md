# User Guide

## Documentation

The documentation site is generated with MkDocs and hosted via GitHub Pages.

## Streamlit App

1. Navigate to the `streamlit/` directory.
2. Install dependencies from `requirements.txt`.
3. Start the app with `streamlit run app.py`.

The app loads sample data from `streamlit/data/` and displays basic CAPEX, lifetime, and efficiency visualizations.
